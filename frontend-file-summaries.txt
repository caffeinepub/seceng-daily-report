{
  "frontend/index.html": "HTML entry point with Google Fonts (Inter + Oswald) preloaded and jsPDF 2.5.1 loaded via CDN script tag for client-side PDF generation.",
  "frontend/src/index.css": "Global styles with dark industrial OKLCH color system; primary token set to dark gray for buttons; separate accent-yellow/accent-orange tokens for safety colors; btn-primary/secondary/danger component classes use dark gray backgrounds (not amber); signature-container utility and dark color-scheme for datetime inputs.",
  "frontend/tailwind.config.js": "Tailwind configuration mapping CSS variable tokens for colors (including accent-yellow and accent-orange), Inter/Oswald/JetBrains Mono fonts, custom border-radius, yellow/orange glow shadow utilities, and fade-in/slide-in/pulse-yellow animations.",
  "frontend/src/utils/pdfGenerator.ts": "PDF generation utility with `ReportData` interface now including `projectManager` and `locationContact` fields; the `generatePDF` function renders both new fields as a two-column row in the Project Information section of the PDF, placed after the Project Address field.",
  "frontend/src/components/ReportForm.tsx": "Daily field report form with `FormState` now including `projectManager` and `locationContact` fields; the Project Information section renders two new labeled text inputs for these fields in a two-column layout after the Project Address input, and both values are passed to the PDF generator.",
  "frontend/src/App.tsx": "Root app component with enlarged header (larger icon, `text-base` title, `text-sm` subtitle) and bigger submenu/hero banner (larger icons, `text-sm` font, increased gap and padding), plus dynamic copyright year.",
  "frontend/src/hooks/useQueries.ts": "Placeholder file confirming no backend queries are needed since the app is fully client-side."
}